name = "render-keepalive"
compatibility_date = "2024-05-01"
compatibility_flags = ["nodejs_compat"]

# 设置定时任务 - 每13分钟执行一次
[[triggers]]
cron = "*/13 * * * *"

# 环境变量（可选）
[vars]
TARGET_URL = "https://mk64-j7xs.onrender.com"

# 日志设置（使用新格式）
[logging]
push = true

[build]
command = ""

