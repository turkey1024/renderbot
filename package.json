{
  "name": "render-keepalive",
  "version": "1.0.0",
  "description": "Cloudflare Worker to keep Render.com free instance alive",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "keywords": ["cloudflare", "worker", "render", "keepalive"],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}

